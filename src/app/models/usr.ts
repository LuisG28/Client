import { Interface } from "readline";
export interface usr{
    id ?: number ;
    nombre ? : string;
    ap ? : string;
    am ? :string;
    pass ? : string;
    estatus ? : string;
    email ? : string ;
    matricula ? : number;
}